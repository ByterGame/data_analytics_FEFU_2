# Задание 2. API-пайплайн: данные → LLM → результат

## Описание

Python-скрипт, который автоматически анализирует медицинские данные о диабете с помощью LLM.

**Задача:** классификация факторов риска диабета и извлечение аналитических инсайтов.

### Пайплайн

```  
results/analysis_result.json
    ^
    | Сохранение
    |
Структурированный JSON (факторы риска, находки, рекомендации)
    ^ 
    | Groq API
    |
CSV-файл (данные о диабете)
```

## Датасет

Diabetes Prediction Dataset — данные о 200 пациентах (выборка из 700 000) с медицинскими показателями: возраст, BMI, давление, холестерин, физическая активность, диета, и др. Целевая переменная — `diagnosed_diabetes` (0/1).

## Инструкция запуска

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка API-ключа

Получите бесплатный API-ключ на [console.groq.com](https://console.groq.com/keys).

```bash
cp .env.example .env
# Отредактируйте .env и вставьте ваш API_KEY
```

### 3. Запуск

```bash
python pipeline.py
```

### 4. Результат

- `results/analysis_result.json` — структурированный JSON от LLM

## Пример входных данных

Файл `data/diabetes_sample.csv` — 200 строк, 26 столбцов:

| id | age | bmi | physical_activity | ... | diagnosed_diabetes |
|----|-----|-----|-------------------|-----|--------------------|
| 0  | 31  | 33.4| 45                | ... | 1                  |
| 1  | 50  | 23.8| 73                | ... | 1                  |

## Пример выходных данных

```json
{
  "dataset_summary": "Датасет содержит медицинские данные 200 пациентов...",
  "risk_factors": [
    {
      "factor": "family_history_diabetes",
      "risk_level": "high",
      "evidence": "Распространённость диабета 86.7% при наличии семейной истории vs 58% без",
      "recommendation": "Таргетный скрининг для лиц с семейным анамнезом"
    }
  ],
  "key_findings": ["Физическая активность — сильнейший модифицируемый фактор..."],
  "demographic_insights": ["Пол и этничность не влияют на риск..."],
  "modeling_recommendations": "Рекомендуется Gradient Boosting (XGBoost)..."
}
```

## Технологии

- Python 3.10+
- Groq API (llama-3.3-70b-versatile)
- Pandas

## Примечание

Подключитесь к VPN перед запуском скрипта
